# 项目地图

先阅读 `PROJECT.md`，它是本项目的目标、边界和权威入口，再依据实际任务读取必要文件。

- 只在本项目根及其子目录工作。`~/ai/archive` 与 Obsidian 仅在任务明确需要某份材料时按路径定点读取，不遍历。
- 先检查已有文件、模板、依赖和最终交付物；不要凭旧项目或目录名推断事实。
- 交付前跑 `ai verify`，确认 `PROJECT.md` 声明的交付物真实可用。未验证的部分要明确说明。
- 项目特有的命令、数据边界、验收步骤和目录惯例写在靠近相关文件的位置，而不是持续扩写本文件。
- 需要专项能力时，只按当前任务从已安装的 Skill 中选择；不要预加载或维护一份全局能力目录。
- 不自动生成过程记录、复盘或平行状态。完成时以实际交付物和相应验证为准。
